// Shared palette for the pre-login (splash + login) screens.
// The brand logo is a warm cream illustration, so the auth screens use the
// app's LIGHT (blush + rose) theme — the logo sits at home, and it still reads
// as the same product as the in-app light theme.
export const AUTH = {
  bgTop: "#FBF4F1",
  bgBottom: "#F4E8EC",
  card: "#F5EFE6", // ~ the logo's own cream, so the illustration blends
  ink: "#2B1F26", // plum text (app light textPrimary)
  inkSoft: "#8B7580", // mauve (app light textSecondary)
  accent: "#D6497E", // app light rose
  onAccent: "#FFFFFF",
  gold: "#B0895A", // echoes the logo's gold script
  symbol: "#D6497E",
  symbolOpacity: 0.16,
};
